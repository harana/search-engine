use harana_common::hashbrown::HashSet;

pub fn entity_tokens(_content: &str) -> HashSet<String> {
    HashSet::new()
    // let str: Vec<String> = _content.split(" ").collect_vec().into_iter().map(|s| s.to_string()).collect();
    // let mut hash = HashSet::new();
    // hash.extend(str);
    // hash
}